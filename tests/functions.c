#include <stdlib.h>
#include <stdio.h>
#include <string.h>

void check(char *file) 
           struct stat lol; 
	if(stat(path, &lol) != 0) { 
	   exit(0);
	   
void check2(char *file) 
        int i = check(file);				
		if(ret) {  
		  printf("File doesn't exist")

int main(int argc, char *argv[])
{
    char **point = &argv;
    check2(&point);
    
    return 0;
}
